# Staged locally via scripts/download-mongod.ps1 + scripts/download-mongo-tools.ps1,
# or automatically in CI (.github/workflows/build-exe.yml).
# Binaries (*.exe) are git-ignored and bundled by electron-builder extraResources.
